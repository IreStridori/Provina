sudo pip install Flask
from flask import flask, render_template, request

app= Flask (__name__)

@app.route("/home")
def home():
  return render_template("home.html")

@app.route("/scelta")
  return render_template("scelta.html")

if __name__== "__main__":
  app.run(debug=True)


<!DOCTYPE html>
<html>
<head>
<title> BACman.home </title>
</head>
